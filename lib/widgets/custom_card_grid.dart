import 'package:flutter/material.dart';

class CustomCardGrid extends StatelessWidget {
  const CustomCardGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
